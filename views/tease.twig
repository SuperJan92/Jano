<article class="tease tease-{{post.type}}" id="tease-{{post.id}}">
	{% block content %}
		<h1 class="h1"><a href="{{post.link}}">{{post.title}}</a></h1>
		<p>{{ post.excerpt }}</p>
		{% if post.thumbnail %}
			<img src="{{post.thumbnail.src}}" />
		{% endif %}
	{% endblock %}
</article>